import eventConf from './event';
import keyMap from './key-map';

// APP 配置
export const appConf = {
  version: '2.0.1',
  index: '/pc/channel/channelManager'
};

// 模块加载
export { eventConf, keyMap };
